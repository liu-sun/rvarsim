% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hgvs_transcribe.R
\name{transcribe_hgvs}
\alias{transcribe_hgvs}
\alias{c_to_g}
\alias{g_to_c}
\title{Map variants between genome and transcript notation}
\usage{
transcribe_hgvs(
  hgvs_strings,
  txdb,
  bsgenome,
  transcript_id = NULL,
  direction = c("c_to_g", "g_to_c", "auto")
)

c_to_g(hgvs_strings, txdb, bsgenome, transcript_id = NULL)

g_to_c(hgvs_strings, txdb, bsgenome, transcript_id = NULL)
}
\arguments{
\item{hgvs_strings}{Character vector of HGVS variant descriptions.}

\item{txdb}{A \code{TxDb} or \code{EnsDb} object providing
transcript structure.}

\item{bsgenome}{A \code{BSgenome} object for sequence context.}

\item{transcript_id}{Optional transcript ID to use when
disambiguating c. notation (required when multiple transcripts
share the same accession).}

\item{direction}{Direction of transcription: \code{"c_to_g"},
\code{"g_to_c"}, or \code{"auto"} (auto-detect from notation).
Default: \code{"auto"}.}
}
\value{
A list of HGVS variant strings in the target notation.
}
\description{
Converts HGVS variants between genomic (g.) and coding (c.) notation
using a TxDb/EnsDb to resolve exon/CDS structure. Handles intronic
offsets, UTR positions, and splice boundary coordinates.
}
\examples{
# Transcribe needs a TxDb with matching BSgenome seqlevels:
if (requireNamespace("EnsDb.Hsapiens.v86", quietly = TRUE) &&
    requireNamespace("BSgenome.Hsapiens.UCSC.hg38", quietly = TRUE)) {
    cat("Ready for transcription mapping")
}
}
